spring:
  profiles:
    include: oauth, aws

  jpa:
    database: H2
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: true
    locations: classpath:db/tests

jwt:
  secret-key: test
  expiration: 3600000
