spring:
  profiles:
    include: oauth, aws
  jpa:
    database: H2
    generate-ddl: false
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  mongodb:
    embedded:
      version: 3.5.5
